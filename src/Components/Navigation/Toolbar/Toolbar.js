import React from "react";
import Logo from "../../Logo/Logo";
import "./Toolbar.scss";

const toolbar = () => (
  <header className="toolbar">
    <div>MENU</div>
    <Logo />
    <nav>...</nav>
  </header>
);

export default toolbar;
